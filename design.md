org-modeでタスク管理している。単に時間を記録しているだけになっているので改良したい。

## 要件

- リードタイムを可視化できるようにする
- あくまでorg-modeの機能を対象とし、ファイルタグ draft は全く関係ない
- たとえば長く WIP を占有しているタスクを見えるようにしたい
- workflow.org は対象外
- 25分未満のクロックは除外
- 最後のクロックインが3ヶ月以内を対象にする
- 最初のクロックインと最後のクロックアウトの差をリードタイムとする

## データソース

- 全 *.org ファイルの TODO/DONE/CLOSE タスクの LOGBOOK(CLOCK エントリ)
- workflow.org は対象外
- 25分未満のクロックは除外

## リードタイムの定義

- リードタイム = 最初のクロックインの日付 〜 最後のクロックアウトの日付(日数)
- 対象: 最後のクロックインが直近3ヶ月以内のタスク

## パイプライン

1. Python スクリプト (`scripts/parse_workflow.py`) が全 org ファイルをパース
2. JSON (`public/workflow-data.json`) を出力
3. HTML ダッシュボード (`public/index.html`) が JSON を読んで可視化

## ダッシュボードの可視化項目

1. リードタイム棒グラフ: タスクごとのリードタイム(日数)を降順表示。長いものがボトルネック候補
2. リードタイム vs 作業時間の散布図: 横軸リードタイム、縦軸実作業時間(分)。右下に位置するタスクは「長期間かかっているが実作業が少ない」= 滞留
3. WIPトレンド: 週ごとのWIP(仕掛かり)数の推移を折れ線グラフで表示。WIPが増え続けていればボトルネックの兆候
4. スループット: 週ごとのDONE完了数を棒グラフで表示。改善が進んでいるか確認
5. リードタイム分布ヒストグラム: 完了タスクのリードタイムの分布。中央値や外れ値が一目でわかる
6. 作業時間ヒートマップ: 曜日×時間帯ごとの作業時間分布。CLOCKのクロックイン時刻から集計
7. フロー効率: 各タスクの実作業時間÷リードタイムを%で表示。低いほど待ち時間が多い
8. エイジングWIP: 現在TODOのタスクの経過日数(最初のクロックインからの日数)。30日超は赤色
9. 累積フロー図(CFD): 週ごとのStarted/Completed/Closedの累積数を積み上げ面グラフで表示。バンドの幅がWIP量
10. ガントチャート: タスクごとの開始日〜終了日を横棒で表示。開始日順にソート。ステータスで色分け
11. バーンチャート: 日ごとの累積Started/Completed数とWIP数を折れ線で表示。Started-Completedの差がWIP
12. 日別タスク別作業時間: 日ごとにどのタスクに何分投入したかをスタック棒グラフで表示
13. 滞留アラート一覧: TODO状態かつリードタイム30日超のタスクを警告表示
14. ファイル別集計テーブル: ファイルごとのタスク数、TODO/DONE/CLOSE内訳、合計作業時間、平均・最大リードタイム。ソート可能
15. タスク一覧テーブル: タスク名、ファイル、ステータス、リードタイム、作業時間、セッション数。ソート可能
